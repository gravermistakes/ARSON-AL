# picks/ dissolution manifest
# generated: 1782416720
category: picks
purpose: injection techniques, bypass payloads, exploitation playbooks — ways in

dissolved:
  - source_repo: bug-reaper
    origin: https://github.com/gravermistakes/bug-reaper
    upstream: https://github.com/shaniidev/bug-reaper
    license: MIT
    classification: web2-exploitation-techniques
    rationale: >
      Bug-reaper is the Web2 4-phase analog of lance. Its per-class
      vulnerability playbooks describe how to exploit a class (injection,
      auth bypass, IDOR, SSRF, etc.) — "ways in" — and the root doc names
      SSTI/SQLi/XSS/XXE + WAF bypass under picks explicitly. So the class
      playbooks + waf-bypass land here; recon/SAST → probes, scope → int,
      validation/report → proofs.
    placed:
      - unit: exploitation-class playbooks
        verb: exploit a specific Web2 bug class
        files: web2/bug-reaper/vulnerabilities/   # 19 classes
      - unit: WAF bypass technique
        verb: evade WAF/defenses
        files: web2/bug-reaper/waf-bypass.md
    cross_refs:
      int: int/web2/bug-reaper      # scope parsing
      probes: probes/web2/bug-reaper # recon + SAST + detection ruleset
      proofs: proofs/web2/bug-reaper # validation + report + chaining + SKILL spine
    notes: >
      subdomain-takeover.md straddles detection/exploitation; kept with its
      class-playbook peers here rather than split.

  - source_repo: fuzz-skill
    origin: https://github.com/gravermistakes/fuzz-skill
    upstream: https://github.com/0xazanul/fuzz-skill
    license: see upstream (no LICENSE in tree)
    classification: memory-bug-fuzzing
    rationale: >
      Fuzzing C/C++ for memory-safety bugs (ASan/UBSan/MSan harnesses, corpus
      planning, crash triage/minimization, exploitability). Fuzzers are "ways
      in" → picks/. Placed whole.
    placed:
      - unit: fuzzing methodology
        verb: harness + fuzz + triage memory bugs
        files: [fuzz-skill/SKILL.md, fuzz-skill/fuzzing-toolchain.md, fuzz-skill/crash-triage.md]
      - unit: worked fuzzer + corpus
        verb: demonstrate the toolchain end to end
        files: fuzz-skill/test-vuln/
        detail: >
          vuln_parser.c + build.sh and 5 sanitizer/coverage builds
          (asan, msan, afl_asan, libfuzzer, cov, prod), seeds/ corpus, and
          fuzz_output/ crash artifacts. KEPT per policy (don't cut fuzzers/corpus).
    notes: >
      test-vuln/ carries ~13M of compiled fuzz harnesses + corpus + crash
      output — intentionally retained as the runnable reference, not stripped.

  - source_repo: ssti-research
    origin: https://github.com/vladko312/Research_Successful_Errors
    license: see upstream
    classification: ssti-injection-technique
    rationale: >
      "Successful Errors" SSTI research — server-side template injection payloads
      and methodology (the paper + slides). SSTI is a named picks/ class. Ways in.
    placed:
      - unit: SSTI payloads + research
        verb: exploit server-side template injection
        files: picks/ssti-research/   # Payloads.md, README, 2 PDFs (paper+slides), images/
    notes: PDFs + images kept per policy (docs/research/examples).

  - source_repo: burp-extensions
    origin: https://github.com/netsquare/Burp-Suite-Extension-Development
    license: see upstream
    classification: attack-extension-dev
    rationale: >
      Burp Suite extension dev examples (IHttpListener intruder/scanner runtime
      encryption, editor-tab runtime crypt). "Custom attack extension generators"
      is a named picks/ unit — tooling to bend the proxy into the attack.
    placed:
      - unit: burp extension examples
        verb: extend Burp for custom attack flows (runtime crypto in intruder/scanner)
        files: picks/burp-extensions/   # *.py (Jython), README
    notes: >
      Jython 2.7 scripts (run inside Burp), not python3 — one uses tabs that
      python3 py_compile rejects; correct under Jython. Kept as-is.
