# probes/ dissolution manifest
# generated: 1782416571
category: probes
purpose: enumeration, fingerprinting, endpoint extraction, detection knowledge

dissolved:
  - source_repo: lance
    origin: https://github.com/gravermistakes/lance
    upstream: https://github.com/shaniidev/lance
    license: MIT
    classification: web3-detection-knowledge
    rationale: >
      Lance gate G2 (Detection) is "what to scan for" — class-based detection
      playbooks and chain-specific guidance. That is probe knowledge. Scope/
      intake went to int/web3, validation/report to proofs/web3.
    placed:
      - unit: detection ruleset (G2)
        action: enumerate exploit classes to check
        files: web3/lance/audit-rules.md
      - unit: vulnerability-class playbooks
        action: detect a specific Web3 bug class
        files: web3/lance/vulnerabilities/   # 13 playbooks
      - unit: chain-specific detection guidance
        action: adjust detection per chain
        files: web3/lance/chains/   # evm, sui-move, cross-chain-bridge, l2
    cross_refs:
      int: int/web3/lance        # scope + intake (G0/G1)
      proofs: proofs/web3/lance  # validation + report (G3-G6) + SKILL spine
    complications: >
      Knowledge only — no scanner binary yet. Peers (noir endpoint extraction,
      vigolium scanner, fuzz-skill) arrive in later dissolutions.

  - source_repo: bug-reaper
    origin: https://github.com/gravermistakes/bug-reaper
    upstream: https://github.com/shaniidev/bug-reaper
    license: MIT
    classification: web2-recon-sast
    rationale: >
      Bug-reaper recon (subdomain enum, fingerprinting, JS mining, endpoint
      discovery) is surface discovery, and source-code-audit is white-box SAST —
      both probes/. The detection ruleset rounds out the probe knowledge.
    placed:
      - unit: recon methodology
        action: discover surface (subdomains, tech, JS, endpoints)
        files: web2/bug-reaper/recon.md
      - unit: white-box SAST methodology
        action: audit source for sinks
        files: web2/bug-reaper/source-code-audit.md
      - unit: detection ruleset
        action: enumerate what to check
        files: web2/bug-reaper/audit-rules.md
    cross_refs:
      int: int/web2/bug-reaper
      picks: picks/web2/bug-reaper
      proofs: proofs/web2/bug-reaper

  - source_repo: drogonsec
    origin: https://github.com/gravermistakes/drogonsec
    upstream: https://github.com/filipi86/drogonsec
    license: Apache-2.0
    classification: sast-sca-secrets-scanner
    rationale: >
      Drogonsec is an executable Go security scanner — SAST (20+ langs), SCA
      (CVE deps), secret leak detection (50+ patterns), and IaC misconfig. That
      is the literal probes/ charter (SAST analyzers, SCA, secret detectors).
      The arsenal's first real scanner binary; placed whole as a coherent unit.
    placed:
      - unit: scanner binary + engines
        action: statically scan source for vulns/CVEs/secrets/IaC
        files: drogonsec/   # cmd/, internal/{engine,analyzer,sca,leaks,ai,reporter,...}, configs, docs/, go.mod, Makefile
        detail: >
          SARIF 2.1 / CWE / CVSS 3.1 output, OWASP Top 10:2025. Internal AI
          remediation (Ollama/cloud) kept with the scanner; it feeds proofs-style
          remediation but is integral to the tool. docs/ kept (install, usage,
          modules, ai-remediation, ci-cd, branch-monitoring, faq, security).
    build: "make build (or go build ./cmd/drogonsec) — go 1.26"
    dropped:
      - report.html, CHANGELOG.md, CLA.md, CODE_OF_CONDUCT.md, CONTRIBUTING.md, SECURITY.md, .claude/
    reason_dropped: generated sample output + governance/IDE files. docs/ kept per policy (keep docs/corpus/examples).

  - source_repo: noir
    origin: https://github.com/owasp-noir/noir
    license: see probes/noir/LICENSE (MIT)
    classification: endpoint-attack-surface-extractor
    rationale: >
      OWASP Noir is a Crystal SAST tool that reads source and extracts the
      endpoints an app exposes — paths, methods, params, headers, cookies, plus
      shadow/deprecated routes — as one inventory. Literal probes/ charter
      (endpoint extractors). The recon-loop surface engine.
    placed:
      - unit: endpoint-extraction engine
        action: parse source -> endpoint inventory
        files: noir/   # src/ (analyzers + tree-sitter grammars), spec/ corpus, docs/, build (shard.yml, Dockerfile, justfile)
        detail: >
          src/ext/tree_sitter/grammars vendors generated parser.c for many langs
          (kotlin 23M, rust, python, ...) — required to parse code, kept whole.
          spec/ test fixtures kept as corpus per policy; docs/ kept.
    build: "shards build (Crystal 1.x); or docker build. github-action/ kept."
    dropped:
      - CODE_OF_CONDUCT.md, CONTRIBUTING.md, SECURITY.md, aur/, snap/, flake.{nix,lock}, shards.nix
    reason_dropped: governance + distro/nix packaging — not functional units.
    notes: ~75M, mostly vendored tree-sitter grammars (integral, not artifacts).

  - source_repo: blockchain-appsec
    origin: https://github.com/OWASP/www-project-blockchain-appsec-standard
    license: CC (see probes/web3/blockchain-appsec/LICENSE.md)
    classification: web3-audit-standard
    rationale: >
      OWASP Blockchain AppSec Standard — verification requirements for blockchain
      apps. Target-audit knowledge (what to check) -> probes/web3, alongside
      lance's detection playbooks. (Distinct from ast10, which governs OUR own
      agent skills in ops/.)
    placed:
      - unit: blockchain appsec verification standard
        action: enumerate web3 security requirements to audit against
        files: probes/web3/blockchain-appsec/   # guides/, index, info
    dropped: [_config.yml, Gemfile, 404.html, CONTRIBUTING.md, SECURITY.md, leaders.md, .github/]
    reason_dropped: Jekyll site scaffolding + governance.

  - source_repo: vigolium
    origin: https://github.com/gravermistakes/vigolium
    upstream: https://github.com/j3ssie/vigolium
    license: see probes/vigolium/LICENSE
    classification: dast-web-vuln-scanner
    rationale: >
      Vigolium is a high-fidelity web vulnerability scanner (Go) — CLI scanner,
      REST API w/ traffic ingestion, 266 active/passive modules, spider/discovery,
      plus an in-process agent engine. The flagship probes/ scanner.
    placed:
      - unit: scanner + modules + agent engine
        action: scan web apps/APIs for vulns; ingest + analyze traffic
        files: probes/vigolium/   # pkg/ (core+modules), internal/, cmd/, public/presets, docs/, test/ fixtures
    build: "make build (go 1.26) -> bin/vigolium. See probes/vigolium/CLAUDE.md."
    dropped: [build/, '*/node_modules', '*/.next', '*/dist']
    reason_dropped: build artifacts only. test/ corpus, docs/, presets, platform/ UI all kept.
    notes: ~67M after excluding build output. Largest tool in the arsenal.

  - source_repo: Web3-Security-Tools
    origin: https://github.com/Quillhash/Web3-Security-Tools
    license: see upstream
    classification: web3-sast-tools
    rationale: >
      EVM static and dynamic audit tools, visualizers, and metrics utilities.
      Categorized as probe/ knowledge/tools.
    placed:
      - unit: SAST tool catalog
        action: list SAST/scanning and visual auditing tools
        files: probes/web3/sast-tools.md

  - source_repo: cosmos-security-handbook
    origin: https://www.faulttolerant.xyz/2024-01-16-cosmos-security-1/
    license: see upstream
    classification: cosmos-chain-security
    rationale: >
      Cosmos SDK security handbook, covering consensus paths, ABCI hooks,
      non-determinism, panic halts, and composite KV key collisions.
      Establishes Cosmos guidance and playbooks in probes/.
    placed:
      - unit: Cosmos SDK chain guide
        action: audit target on Cosmos chains
        files: probes/web3/lance/chains/cosmos.md
      - unit: Cosmos playbooks
        action: detect Cosmos-specific bug classes
        files:
          - probes/web3/lance/vulnerabilities/cosmos-nondeterminism.md
          - probes/web3/lance/vulnerabilities/cosmos-abci-panic.md
          - probes/web3/lance/vulnerabilities/cosmos-kv-key-collision.md

