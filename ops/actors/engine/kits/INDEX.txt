OPACA WEB3 HUNTING KITS - COMPLETE DELIVERABLES
================================================

Location: /home/user/ARSON-AL/ops/actors/engine/kits/

CORE IMPLEMENTATION (C++20, Header-Only)
========================================

1. web3.hpp (363 lines)
   - Web3 kit definitions: 21 kits across 5 engagement phases
   - Finding types: 19 types (CWE-mapped)
   - Tool lists: Kit-to-binary mappings
   - Crosslink weights: Soft-weighting for natural kit chaining
   - Level gating: Actor levels 0-8, exponential penalty below min
   - Lance gate mapping: Each kit → 1-2 gates
   
   Enums:
     • FindingType (REENTRANCY, FLASH_LOAN, ORACLE_MANIP, SLASHING_EVASION, etc.)
     • KitId (21 kits: CONTRACT_AUDIT, DEFI_SCAN, ..., BRIDGE_SIDECHAIN)
   
   Data:
     • kit_to_lance_gate: array mapping each kit to lance gates
     • kit_tools: unordered_map<KitId, vector<string>>
     • kit_crosslinks: unordered_map<CrosslinkKey, double> (18 explicit weights)
     • kit_min_level: constexpr function per kit

2. kit_switch.hpp (284 lines)
   - Deterministic kit selection algorithm
   - Pure function: select_kit(uuid, timestamp, hunt_state, xlinks) → KitId
   - No LLM, no side effects, O(21) complexity
   
   Structs:
     • HuntState (findings, phase, actor_level, hunt_elapsed_sec)
     • CrosslinkSnapshot (weights pointer, default weight)
   
   Functions:
     • uuid_bit_range(): Extract bits from 13-digit septal UUID
     • uuid_preferred_kit(): Bias kit selection toward UUID-preferred kit
     • score_kit(): Scoring function (phase, xlink, UUID, level, time)
     • select_kit(): Main selection (find best-scoring kit for this actor)

DOCUMENTATION
==============

3. CLAUDE.md (33 lines)
   - Quickstart: what's here, build commands, test approach, feeds, issues
   - Under 50-line limit per ARSON-AL standards
   - Unix epoch timestamp: 1719604800

4. README.md (209 lines)
   - Comprehensive overview of kit system
   - Architecture, actor lifecycle, kit phases
   - Crosslinks, level gating, selection algorithm
   - Quick start, compilation, testing guidance

5. DIRECTORY-MAPPING.md (199 lines)
   - Complete mapping: kit → ARSON-AL directory
   - Directory inventory (int/, probes/, picks/, proofs/, paths/)
   - Lance 7-gate flow with examples
   - Crosslink examples, anti-gaming notes

6. COSMOS-SLASHING-LEAD.md (162 lines)
   - P1 vulnerability: Cosmos SDK CancelUnbondingDelegation OnHold() bypass
   - CVSS 9.8, affects 50+ ICS chains
   - Full attack sequence with Rust PoC template
   - Kit chain walkthrough (COSMOS_PROBE → IMMUNEFI_REPORT)
   - Detection rules, actor behavior simulation
   - Bounty platform: Immunefi ($50k–$500k)
   - Tools needed for dissolution

COMPILATION & VERIFICATION
===========================

✓ clang++ -std=c++20 web3.hpp -fsyntax-only (zero warnings)
✓ clang++ -std=c++20 kit_switch.hpp -fsyntax-only (zero warnings)
✓ Dependencies: C++20 stdlib only (cstdint, string, array, unordered_map, algorithm, cmath)
✓ Header-only (no .cpp implementation needed)
✓ Linked directly into OPACA actor runtime

CONTENT SUMMARY
===============

Kits:        21 (recon/scan/exploit/validate/report + persist)
Finding Types: 19 (CWE-mapped)
Lance Gates:  7 (scoping → reporting)
Tool Refs:    ~90 (from int/, probes/, picks/, proofs/, paths/)
Crosslinks:   18 explicit + 0.3 default
Level Gating: 0-8 (soft penalty below min_level)
Total Lines:  1,041 (363 C++, 284 C++, 394 markdown)

INTEGRATION POINTS
==================

1. OPACA Actor Runtime
   - Include web3.hpp + kit_switch.hpp
   - At actor spawn: HuntState state = {...};
   - Per iteration: select_kit(actor.uuid, now(), state, xlinks);
   - Load kit → run tools → emit findings → update state

2. Kit Registry (to be built)
   - Load kit_tools[kit_id] to get binary names
   - Spawn tools as Riot processes
   - Pipe findings back to actor

3. MCP Server (planned)
   - Expose switch_kit, get_findings, submit_report as MCP tools
   - Agent can inspect kit state, but actors run autonomously

NEXT STEPS (OUT OF SCOPE)
=========================

- OCaml + Riot runtime bindings
- Kit registry + tool loader implementation
- Integration testing with actual Cosmos SDK chains
- Immunefi report generator output formatting
- Memory persistence for crosslink weights

