# advanced_evolution (pointer)

Canonical tree: `ops/actors/advanced_evolution/` — it runs as an actor (an
LLM-driven evolutionary loop). It is referenced here in `mem/` because its job
is learned memory: evolving kit / payload / probe configs from scored engagement
history. One component, two roles ("optimizer is memory and actors").
