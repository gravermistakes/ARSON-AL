# proofs/ dissolution manifest
# generated: 1782416571
category: proofs
purpose: exploit validation, economic models, triage, scoring, report generation

dissolved:
  - source_repo: lance
    origin: https://github.com/gravermistakes/lance
    upstream: https://github.com/shaniidev/lance
    license: MIT
    classification: web3-validation-report
    rationale: >
      Lance's center of gravity is validation: 4 of its 7 gates (G3 Exploit,
      G4 Economic, G5 False-Positive, G6 Triage/Report) gate findings down to
      reportable. That, plus the 7-gate orchestration spine and the report
      generators, is proofs/. Scope/intake → int/web3, detection → probes/web3.
    placed:
      - unit: 7-gate methodology spine
        action: orchestrate scope→detection→validation→report
        files: [web3/lance/SKILL.md, web3/lance/workflow.md]
      - unit: validation gate references (G3-G6)
        action: prove exploitability / economics / reject FPs / simulate triage
        files: web3/lance/refs/   # exploit, economic, false-positive, triage, severity-guide
      - unit: platform triage rules
        action: simulate per-platform triage
        files: web3/lance/platforms/   # immunefi, hackenproof, hackerone-web3, bugcrowd-web3
      - unit: report templates + finding schema
        action: render platform-ready reports
        files: web3/lance/templates/
      - unit: scoring/triage/report/adapter scripts
        action: score CVSS, simulate triage, generate report, adapt invariant output
        files: web3/lance/scripts/   # scoring_engine, triage_simulator, generate_web3_report, invariant_output_adapter
      - unit: per-agent skill adapters
        action: deliver the lance skill to agents
        files: web3/lance/agents/
    cross_refs:
      int: int/web3/lance        # G0/G1 scope + intake
      probes: probes/web3/lance  # G2 detection knowledge
    dropped:
      - README.md, CHANGELOG.md, CONTRIBUTING.md, lance.png, .gitignore
    reason_dropped: upstream packaging / marketing.
    complications: >
      The SKILL spine references scripts/parse_web3_scope.py and
      scripts/normalize_targets.py, which dissolved to int/web3/lance/scripts/.
      Paths left stale on purpose (rewriting couples dirs); MANIFEST cross-links
      record the true locations. Python kept as-is.

  - source_repo: bug-reaper
    origin: https://github.com/gravermistakes/bug-reaper
    upstream: https://github.com/shaniidev/bug-reaper
    license: MIT
    classification: web2-validation-report
    rationale: >
      Bug-reaper Phase 3 (Validation) + Phase 4 (Report) gate findings to
      reportable P2+, plus the exploit-chain builder. Center of gravity → proofs/.
      The 4-phase SKILL spine lives here; scope → int/web2, recon/SAST →
      probes/web2, exploitation → picks/web2.
    placed:
      - unit: 4-phase methodology spine
        action: orchestrate recon→detect→validate→report
        files: web2/bug-reaper/SKILL.md
      - unit: validation gate references
        action: prove exploitability / reject FPs / score severity
        files: web2/bug-reaper/refs/   # exploit-validation, false-positive-elimination, severity-guide
      - unit: exploit chain builder
        action: braid low-sev findings into a critical chain
        files: web2/bug-reaper/refs/chaining.md
      - unit: platform triage rules
        action: simulate per-platform triage
        files: web2/bug-reaper/platforms/   # hackerone, bugcrowd, intigriti, yeswehack
      - unit: report generator
        action: render platform-ready report
        files: web2/bug-reaper/scripts/generate_report.py
    dropped:
      - README.md, CHANGELOG.md, .gitignore
    reason_dropped: upstream packaging.
    complications: >
      SKILL spine references scripts/analyze_scope.py (now int/web2) and the
      recon/SAST refs (now probes/web2) by old paths — cross-linked, not
      rewritten. chaining.md realizes ARSENAL's "Chain loop". Python kept.
