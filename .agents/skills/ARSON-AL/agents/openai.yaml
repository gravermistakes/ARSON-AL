interface:
  display_name: "ARSON AL"
  short_description: "Repo-specific patterns and workflows for ARSON-AL"
  default_prompt: "Use the ARSON-AL repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true